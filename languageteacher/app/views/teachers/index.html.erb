<h1>Listing teachers</h1>

<table>
    <tr>
        <th>Title</th>
        <th>Text</th>
    </tr>

    <% @teachers.each do |teacher| %>
        <tr>
            <td><%= teacher.title %></td>
            <td><%= teacher.text %></td>
        </tr>
    <% end %>
</table>